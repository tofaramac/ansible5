hostname: "{{ sts[1].hostname }}"
ansible_ssh_host: "{{ sts[1].ipaddress }}"
ip_allocation: "{{ sts[1].ipallocation }}"
ext_nat: "{{ sts[1].ext_nat }}"

mntserver: "{{ mounts[1].mntserver }}"
mntserverip: "{{ mounts[1].mntserverip }}"
mnttarget: "{{ mounts[1].mnttarget }}"
mountpoint: "{{ mounts[1].mntpoint }}"
mnttype: "{{ mounts[1].mnttype }}"

rsyslogserver: "{{ infra[1].ipaddress }}"