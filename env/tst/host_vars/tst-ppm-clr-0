hostname: "{{ mariadb[0].hostname }}"
ansible_ssh_host: "{{ mariadb[0].ipaddress }}"
ip_allocation: "{{ mariadb[0].ipallocation }}"
ext_nat: "{{ mariadb[0].ext_nat }}"

mntserver: "{{ mounts[0].mntserver }}"
mntserverip: "{{ mounts[0].mntserverip }}"
mnttarget: "{{ mounts[0].mnttarget }}"
mountpoint: "{{ mounts[0].mntpoint }}"
mnttype: "{{ mounts[0].mnttype }}"

rsyslogserver: "{{ infra[0].ipaddress }}"