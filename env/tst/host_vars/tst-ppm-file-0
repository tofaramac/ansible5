hostname: "{{ file[0].hostname }}"
ansible_ssh_host: "{{ file[0].ipaddress }}"
ip_allocation: "{{ file[0].ipallocation }}"
ext_nat: "{{ file[0].ext_nat }}"

# mounts NAS
mntserver: "{{ nasmounts[0].mntserver }}"
mntserverip: "{{ nasmounts[0].mntserverip }}"
mnttarget: "{{ nasmounts[0].mnttarget }}"
mountpoint: "{{ nasmounts[0].mntpoint }}"
mnttype: "{{ nasmounts[0].mnttype }}"

rsyslogserver: "{{ infra[0].ipaddress }}"